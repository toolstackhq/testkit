import { defineConfig } from "allure";

export default defineConfig({
  name: "qa-patterns Playwright Template",
  plugins: {
    awesome: {
      options: {
        singleFile: true
      }
    }
  }
});
